// Entry point. Importing game.js evaluates it, which wires input + the loop and bootstraps the
// first frame. Kept as a thin entry so index.html has a single, stable module to load.
import './game.js';
